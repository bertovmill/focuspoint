// Section content lives in the persistent (app) layout shell, which reads the
// pathname to decide what to show. This page exists to make /vision a real route.
export default function Page() {
  return null;
}
