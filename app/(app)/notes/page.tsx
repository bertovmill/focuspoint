// Section content lives in the persistent (app) layout shell, which reads the
// pathname to decide what to show. This page exists to make /notes a real route.
export default function Page() {
  return null;
}
