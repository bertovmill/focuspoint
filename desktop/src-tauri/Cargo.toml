[package]
name = "focuspoint-desktop"
version = "0.1.0"
description = "Focuspoint desktop shell"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[target.'cfg(target_os = "macos")'.dependencies]
objc = "0.2"

[profile.release]
strip = true
lto = true
